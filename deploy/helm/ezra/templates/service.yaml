apiVersion: v1
kind: Service
metadata:
  name: {{ include "ezra.fullname" . }}
  labels: {{- include "ezra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "ezra.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
